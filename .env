PORT=5000
FLASK_DEBUG=True

# SQL Server Connection (change these)
SERVER_NAME=your-server-name    # e.g. localhost or server IP
DATABASE_NAME=your_database_name
AUTH_TYPE=sql                   # "sql" or "windows"
USERNAME=your_username          # only if using SQL auth
PASSWORD=your_password          # only if using SQL auth